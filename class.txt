No code & Low Code
AppSheet
Power plat PowerAPP

RPA(자동화) -> 생성형AI

파이선의 생산성 - 26개정도의 키워드로 다른 언어에 비해 적다.

디지털 리터러시(문법) - 기계랑 대화하는 시대

AWS, Azure, GCP 전체 클라우드의 3대장 (Java, JS, Python)

머니트랜드,  ??트랜드

통계학전공자가 프로그래밍이 가능하다면 잘 팔린다.

아나콘다 - 200인이상의 사용자에 대해서는 라이선스를 구매해야함.

* dict:
1. ~.items() : key, value
2. ~.keys() : key
3. ~.values() : value

Python은 축약어를 많이 사용함.

파이썬 - 모든것이 객체다.

파이썬 전역공간 
스코핑룰 : (LGB) Local, Global, Bultin 변수 우선순위


람다함수 - 익명함수, 일회성함수, 즉흥적함수 
            a:a>20
         (입력):(처리)

클래스와 객체
1. 쿠키틀, 붕어빵틀 : 탬플릿, 원본, 청사진
2. 객체(object, instance) : 실제 메모리에서 실행
3. 메서드 호출

파이썬은 36개의 키워드(예약어)가 있다.  + Library(모듈, 패키지) 200개 + 외부

함수(3장):1개 가능
클래스(4장) : N개 변수 + N개 매서드
모듈(4장) : 1개 File

   패키지명.모듈명.함수()
    (폴더)   (파일명)

패키지인지 모듈인지는 개발자의 가이드를 따라하면된다.

lib 디렉토리에 모듈(파이썬파일)을 복사하면
?/lib/_pycache_/ 밑에 모듈의 이진코드도 아닌 이상한 파일(바이트코드)이 생성된다.

* file
1. read() => str변수(1~10)
2. readlines() => list변수(500줄) 
3. readline() => 500MB ~ 1GB, (log, dataset) 

dir(str) -  str 클래스에 어떤 메서드가 있는지 보자...

import os.path 로 선언하면 메모리에 os.path라는 방이 만들어지고 메서드를 호출시 os.path.메서드()와 같이 호출한다.
그러나 from os.path import * 로 선언하면 메모리에 방이 만들어지지 않고, 그냥 메서드()만 호출하면 된다.


웹에서 .이라는 것은 class속성과 매핑된다.

시그널 - 신호(이벤트)
슬롯 - 이벤트 핸들러






